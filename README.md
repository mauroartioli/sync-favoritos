# Sync Favoritos (Safari ➜ Edge Cloud)

Este repositório contém:

- **v1/**: a solução atual (funcionando hoje), transparente e sem UI (apenas o ícone da extensão no Edge).
- **v2/**: a evolução para “aplicativo real” no macOS (instalador + serviço/LaunchAgent + documentação própria).

## Começar

- Para usar a solução atual: veja `v1/README.md`.
- Para acompanhar/instalar a nova versão: veja `v2/README.md`.

